export const dynamic = 'force-dynamic';
export const revalidate = 0;

import GeneralAdminClient from './general-admin-client';

export default function GeneralAdminPage() {
  return <GeneralAdminClient />;
}